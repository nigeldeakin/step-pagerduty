name: pagerduty-notifier
version: 1.0.10
summary: Send an event to PagerDuty.
tags:
  - notification
  - webhook
  - pagerduty
properties:
# If set to failed, this step will only send an event if the pipeline failed.
- name: notify_on
  type: string
  required: false
# URL to send the event to. 
- name: url
  type: string
  required: false
  default: https://events.pagerduty.com/generic/2010-04-15/create_event.json
# If set to the name of a branch, this step will only send an event for runs on that branch
- name: branch
  type: string
  required: false
- name: service_key
  type: string
  required: true
# if set, description is prepended to the event message.
- name: description
  type: string
  required: false
- name: client
  type: string
  required: false
  default: pagerduty-notifier
- name: client_url
  type: string
  required: false
